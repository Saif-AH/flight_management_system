namespace FlightManagementSystem.Domain.Enums;

public enum FlightStatus
{
    Scheduled = 1,
    Delayed = 2,
    Cancelled = 3,
    Completed = 4
}

public enum BookingStatus
{
    Active = 1,
    Cancelled = 2
}